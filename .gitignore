# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Recording artifacts
captures/
output/

# `h2v review --out review.html` written to cwd. (The default serves from
# memory — no file; --no-serve uses a tmpfile. Neither is affected by this.)
review.html

# OS
.DS_Store
Thumbs.db

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo

# Claude Code: local/transient state. Project-shared config under .claude/
# (settings.json, commands/, agents/, hooks/, skills/) is intentionally NOT
# ignored — commit those when they exist.
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/.cache/
