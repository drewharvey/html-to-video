# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Recording artifacts
captures/
output/

# Output of `h2v review` when written to cwd (default tmpfile is unaffected)
review.html

# OS
.DS_Store
Thumbs.db

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo

# Claude Code: local/transient state. Project-shared config under .claude/
# (settings.json, commands/, agents/, hooks/, skills/) is intentionally NOT
# ignored — commit those when they exist.
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/.cache/
