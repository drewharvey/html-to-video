{
  "name": "html-to-video",
  "version": "0.1.0",
  "description": "Record HTML animations as 4K MP4s. Drop a file in a folder, run `h2v export`, get a video.",
  "bin": {
    "h2v": "./cli.js",
    "html-to-video": "./cli.js"
  },
  "main": "./cli.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "export": "node ./cli.js export"
  },
  "dependencies": {
    "puppeteer": "^24.42.0"
  }
}
